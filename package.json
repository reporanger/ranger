{
  "name": "issue-bot",
  "version": "1.0.0",
  "description": "",
  "author": "Mike Fix <mrfix84@gmail.com> (dawnlabs.io)",
  "license": "ISC",
  "repository": "https://github.com/mfix22/issue-bot.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage",
    "redis:clear": "redis-cli flushall"
  },
  "dependencies": {
    "bee-queue": "^1.2.2",
    "moment": "^2.22.1",
    "ms": "^2.1.1",
    "probot": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
